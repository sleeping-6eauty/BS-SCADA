<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.example.backend.domain.User">
        <id property="userId" column="user_id" />
        <result property="name" column="name" />
        <result property="email" column="email" />
        <result property="passwordHash" column="password_hash" />
        <result property="role" column="role" />
        <result property="status" column="status" />
    </resultMap>

    <select id="findByEmail" resultMap="userResultMap" parameterType="string">
        SELECT user_id, name, email, password_hash, role, status
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="userResultMap" parameterType="long">
        SELECT user_id, name, email, password_hash, role, status
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="findAll" resultMap="userResultMap">
        SELECT user_id, name, email, password_hash, role, status
        FROM users
        <where>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY user_id
    </select>

    <insert id="insert" parameterType="com.example.backend.domain.User"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (name, email, password_hash, role, status)
        VALUES (#{name}, #{email}, #{passwordHash}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.example.backend.domain.User">
        UPDATE users
        SET name = #{name}, email = #{email}, password_hash = #{passwordHash}, role = #{role}, status = #{status}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

</mapper>
